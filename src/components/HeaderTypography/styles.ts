import { styled } from '@mui/material/styles';

const Box = styled('div')(() => ({
    justifyContent: 'center',
    alignContent: 'center',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
}));

export const styles = { Box };
// "bracketLine": true
